# ===========================================
# Production Environment Variables for Railway
# ===========================================

# Database (Railway provides this automatically)
DATABASE_URL=postgresql://user:password@host:5432/railway

# Clerk Authentication (Production keys from Clerk Dashboard)
CLERK_SECRET_KEY=sk_live_your_clerk_secret_key
VITE_CLERK_PUBLISHABLE_KEY=pk_live_your_clerk_publishable_key

# AI Provider (openai | gemini)
AI_PROVIDER=openai
OPENAI_API_KEY=sk-your-openai-api-key
# GOOGLE_GENERATIVE_AI_API_KEY=your-gemini-api-key

# Frontend API URL (Railway backend URL)
VITE_API_URL=https://your-backend-service.up.railway.app

# CORS Origin (Railway frontend URL)
CORS_ORIGIN=https://your-frontend-service.up.railway.app

# ===========================================
# Railway Configuration Notes:
# ===========================================
# 1. Create a new project in Railway
# 2. Add PostgreSQL plugin (DATABASE_URL auto-configured)
# 3. Deploy backend service from packages/backend
# 4. Deploy frontend service from packages/frontend
# 5. Set environment variables in Railway dashboard
# 6. Update Clerk Dashboard:
#    - Add Railway URLs to "Allowed Origins"
#    - Use production keys (sk_live_, pk_live_)
